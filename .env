# Database Type: "mongodb" or "postgres"
DB_TYPE=mongodb

# MongoDB Configuration (used when DB_TYPE=mongodb)
MONGO_URI=mongodb+srv://TempMail:TempMail@cluster0.2xrxd0k.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
DB_NAME=tempmail

# PostgreSQL Configuration (used when DB_TYPE=postgres)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres

# HTTP Server
PORT=3000

# SMTP Server
SMTP_PORT=2525
MAIL_DOMAIN=tempmail.dev

# CORS - Frontend origin
CORS_ORIGIN=http://localhost:5173

# Webhook Base URL (set to your public domain in production)
WEBHOOK_BASE_URL=http://localhost:3000
